{% form_theme form 'bootstrap_5_layout.html.twig' %}

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-dark my-2"><i class="fa fa-plus" aria-hidden="true"></i> Add a strategy</button>
{{ form_end(form) }}

       {# <div class="row">
            <div class="col-6">
                <button class="btn btn-outline-dark w-100">{{ button_label|default('Save') }}</button>
                {{ form_end(form) }}

            </div>
            <div class="col-6">
                {% if button_label is defined and button_label == "Update" %}
                    {{ include('trade/_delete_form.html.twig') }}
                {% else %} 
                    <button class="btn btn-danger w-100" disabled>Delete</button>

                {% endif %}

            </div>
        </div> #}
