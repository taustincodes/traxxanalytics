{% form_theme tradeForm 'bootstrap_5_layout.html.twig' %}

{{ form_start(tradeForm) }}
    {{ form_widget(tradeForm) }}
       <div class="row">
            <div class="col-6">
                <button class="btn btn-outline-dark w-100">{{ button_label|default('Save') }}</button>
                {{ form_end(tradeForm) }}

            </div>
            <div class="col-6">
                {% if button_label is defined and button_label == "Update" %}
                    {{ include('trade/_delete_form.html.twig') }}

                {% endif %}

            </div>
        </div>
