<form method="post" action="{{ path('app_trade_delete', {'id': trade.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trade.id) }}">
    <button class="btn btn-danger w-100">Delete</button>
</form>
